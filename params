SELECT c.status, COUNT(1) AS cnt FROM c
WHERE c.createdTimeStamp >= "..." AND c.createdTimeStamp <= "..."
GROUP BY c.status

SELECT VALUE SUM(c.amount) FROM c
WHERE c.ledgerId = "a0c1b2d3-e4f5-6789-abcd-ef0123456789"

The response-time curve is the soak result. Starts at ~1.35s, drops through ~0.5s by 10:08, settles around 0.25–0.3s by 10:12 and stays flat for the remaining 24 minutes. 
No upward drift as 53,848 documents accumulated. That's the pass condition — a soak is looking for degradation over time, and there isn't any.
